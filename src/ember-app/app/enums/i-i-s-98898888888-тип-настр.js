import { createEnum } from 'ember-flexberry-data/utils/enum-functions';

export default createEnum({
  ВыборХост: 'ВыборХост',
  УстОС: 'УстОС',
  УстСервПО: 'УстСервПО',
  НастрСети: 'НастрСети'
});
